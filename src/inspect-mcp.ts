import { resolveExactMCPServer } from "./mcp-exact-resolution.js";
import { MCPRemoteCapabilityInspector } from "./inspection.js";
import { classifyMCPTool, classifyWebSearch } from "./classification.js";
const names=process.argv.flatMap((v,i,a)=>v==="--server"?[a[i+1]]:[]).filter((v):v is string=>!!v);
if(!names.length||names.length>3||process.argv.some(v=>/^--(?:all|search|possible|top|scan)/.test(v))){console.error("Usage: npm run inspect:mcp -- --server <exact-registry-name> [--server <exact-registry-name>] (max 3)");process.exitCode=2;}else{const base=process.env.MCP_REGISTRY_URL??"https://registry.modelcontextprotocol.io";for(const name of names){const resolved=await resolveExactMCPServer(name,base);console.log(JSON.stringify({requested:name,registryUrl:resolved.url,resolution:resolved.status,version:resolved.record?.mcp?.version,status:resolved.record?.mcp?.status,selectedRemote:resolved.record?.mcp?.remotes.find(x=>x.type==="streamable-http")?.url}));if(resolved.status!=="success"||!resolved.record)continue;const item=resolved.record,before=classifyWebSearch(item),inspection=await new MCPRemoteCapabilityInspector().inspect(item);console.log(JSON.stringify({server:item.externalResourceId,registryClassification:before.status,inspection:{status:inspection.status,protocolVersion:inspection.protocolVersion,tools:inspection.capabilities.map(t=>({name:t.nativeName,description:t.description,classification:classifyMCPTool(item.externalResourceId,t)}))},safety:{toolsCall:0,payments:0}},null,2));}}
