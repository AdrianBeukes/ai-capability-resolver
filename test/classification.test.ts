import assert from "node:assert/strict"; import test from "node:test";
import { classifyWebSearch } from "../src/classification.js"; import { normalizeX402DiscoveredResource } from "../src/external-discovery.js"; import { normalizeMCPRegistryServer } from "../src/mcp-registry-discovery.js";
function x(description:string,input?:object,output?:object,url="https://api.test/search"){return normalizeX402DiscoveredResource({resource:url,type:"http",x402Version:2,accepts:[{scheme:"exact",network:"eip155:1",amount:"1",asset:"a",payTo:"p",maxTimeoutSeconds:1}],lastUpdated:"2026-01-01T00:00:00Z",description,extensions:{bazaar:{info:{input,output}}}},"x","now")!;}
test("matches strong general web-search metadata",()=>{assert.equal(classifyWebSearch(x("Real-time web search across the internet",{query:"text"},{results:[{url:"url"}]})).status,"matched"); assert.equal(classifyWebSearch(x("Neural search across the web",{query:"string"},{results:[{url:"string"}]})).status,"matched");});
test("keeps incomplete public-web claims possible",()=>assert.equal(classifyWebSearch(x("Public web search",undefined,undefined)).status,"possible"));
test("rejects scoped and weak search false positives",()=>{for(const d of ["Search Twitter/X tweets","Google Trends keyword search","Business directory search and maps","Documentation search","Search things"]) assert.notEqual(classifyWebSearch(x(d)).status,"matched");});
test("classifies MCP through same API without inventing tools",()=>{const m=normalizeMCPRegistryServer({server:{name:"example/web",version:"1",description:"Public web search"}},"m","now")!; const r=classifyWebSearch(m); assert.equal(r.status,"possible"); assert.equal(r.missingEvidence.includes("compatible textual-query input schema"),true);});
