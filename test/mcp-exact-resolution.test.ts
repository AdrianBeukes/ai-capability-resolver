import assert from "node:assert/strict";import test from "node:test";import {resolveExactMCPServer}from "../src/mcp-exact-resolution.js";
const raw={server:{name:"agency.goji/goji",version:"1.0.1",remotes:[{type:"streamable-http",url:"https://goji.test/mcp"}]},_meta:{"io.modelcontextprotocol.registry/official":{status:"active"}}};
test("resolves only exact encoded latest version route",async()=>{let url="";const r=await resolveExactMCPServer("agency.goji/goji","https://registry.test",(async x=>{url=String(x);return new Response(JSON.stringify(raw))})as typeof fetch);assert.equal(r.status,"success");assert.match(url,/servers\/agency.goji%2Fgoji\/versions\/latest$/);assert.ok(!url.includes("search"));});
test("404 and mismatched identity fail closed",async()=>{assert.equal((await resolveExactMCPServer("x","https://r",(async()=>new Response("",{status:404}))as typeof fetch)).status,"exact_identity_not_found");assert.equal((await resolveExactMCPServer("x","https://r",(async()=>new Response(JSON.stringify(raw)))as typeof fetch)).status,"identity_mismatch")});
